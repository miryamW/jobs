import { ShowDomainPipe } from './show-domain.pipe';

describe('ShowDomainPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowDomainPipe();
    expect(pipe).toBeTruthy();
  });
});
